diff -r 944f5067d04a contrib/lighttpd-1.4.31/src/fdevent.c
--- a/contrib/lighttpd-1.4.31/src/fdevent.c	Wed Sep 19 11:47:39 2012 +0200
+++ b/contrib/lighttpd-1.4.31/src/fdevent.c	Wed Sep 19 11:53:15 2012 +0200
@@ -198,11 +198,16 @@
 }
 
 int fdevent_fcntl_set(fdevents *ev, int fd) {
+#if 0
 #ifdef FD_CLOEXEC
 	/* close fd on exec (cgi) */
 	fcntl(fd, F_SETFD, FD_CLOEXEC);
 #endif
-	if ((ev) && (ev->fcntl_set)) return ev->fcntl_set(ev, fd);
+	if ((ev) && (ev->fcntl_set)){
+		fprintf(stderr, "call ev->fcntl_set(ev, %d)\n", fd);
+		return ev->fcntl_set(ev, fd);
+	}
+#endif
 #ifdef O_NONBLOCK
 	return fcntl(fd, F_SETFL, O_NONBLOCK | O_RDWR);
 #else
diff -r 944f5067d04a contrib/lighttpd-1.4.31/src/fdevent_poll.c
--- a/contrib/lighttpd-1.4.31/src/fdevent_poll.c	Wed Sep 19 11:47:39 2012 +0200
+++ b/contrib/lighttpd-1.4.31/src/fdevent_poll.c	Wed Sep 19 11:53:15 2012 +0200
@@ -193,7 +193,7 @@
 int fdevent_poll_init(fdevents *ev) {
 	UNUSED(ev);
 
-	log_error_write(srv, __FILE__, __LINE__,
+	log_error_write(ev->srv, __FILE__, __LINE__,
 		"s", "poll is not supported, try to set server.event-handler = \"select\"");
 
 	return -1;
diff -r 944f5067d04a contrib/lighttpd-1.4.31/src/http-header-glue.c
--- a/contrib/lighttpd-1.4.31/src/http-header-glue.c	Wed Sep 19 11:47:39 2012 +0200
+++ b/contrib/lighttpd-1.4.31/src/http-header-glue.c	Wed Sep 19 11:53:15 2012 +0200
@@ -175,6 +175,7 @@
 			break;
 #endif
 		case AF_INET:
+#if 0
 			if (NULL == (he = gethostbyaddr((char *)&our_addr.ipv4.sin_addr, sizeof(struct in_addr), AF_INET))) {
 				log_error_write(srv, __FILE__, __LINE__,
 						"SdS", "NOTICE: gethostbyaddr failed: ",
@@ -184,6 +185,8 @@
 			} else {
 				buffer_append_string(o, he->h_name);
 			}
+#endif
+			buffer_append_string(o, inet_ntoa(our_addr.ipv4.sin_addr));
 			break;
 		default:
 			log_error_write(srv, __FILE__, __LINE__,
diff -r 944f5067d04a contrib/lighttpd-1.4.31/src/network.c
--- a/contrib/lighttpd-1.4.31/src/network.c	Wed Sep 19 11:47:39 2012 +0200
+++ b/contrib/lighttpd-1.4.31/src/network.c	Wed Sep 19 11:53:15 2012 +0200
@@ -300,6 +300,7 @@
 		if (host == NULL) {
 			srv_socket->addr.ipv4.sin_addr.s_addr = htonl(INADDR_ANY);
 		} else {
+#if 0
 			struct hostent *he;
 			if (NULL == (he = gethostbyname(host))) {
 				log_error_write(srv, __FILE__, __LINE__,
@@ -319,6 +320,7 @@
 			}
 
 			memcpy(&(srv_socket->addr.ipv4.sin_addr.s_addr), he->h_addr_list[0], he->h_length);
+#endif
 		}
 		srv_socket->addr.ipv4.sin_port = htons(port);
 

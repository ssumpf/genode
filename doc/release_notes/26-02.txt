

              ===============================================
              Release notes for the Genode OS Framework 26.02
              ===============================================

                               Genode Labs



; intro
; @nfeske

Human-inclined data (HID) format by default
###########################################

; @nfeske

commit 7b0fa18e763a33bf5a89f3477697b5819a0dff30
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Tue Sep 30 15:09:11 2025 +0200

    sculpt.run: interoperability with both XML and HID
    
    This patch makes sculpt.run accept launchers and deploy files in both
    formats XML and HID.
    
    Issue #5710

commit 597457ad02dc2306295ad810f81f813952b4548d
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Mon Jan 5 14:26:27 2026 +0100

    tool/hid: try to preserve XML comments
    
    The regular expression used for capturing (and dropping) XML comments
    was limited to single-line comments. This patch enhances the pattern
    to capture multi-line comments, and tries to preserve the captured
    information, either as disabled node or as an textual comment.
    
    Observed with repos/gems/sculpt/launcher/mixer.
    
    Fixes #5782

commit 7bc0160788e16a24652af513b67fe9899084a582
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Mon Jan 5 16:27:09 2026 +0100

    tool/hid: work around Tcl's string trim
    
    Tcl's built-in 'string trim' function seems to produce invalid UTF-8
    sequences for certain strings. This patch sidesteps this problem
    by using a regexp for trimming.
    
    Observed with repos/os/src/server/event_filter/fr_bepo.chargen.
    
    Issue #5782

commit d11570e06a2d091e91fe8e795882bd90b43b0f6a
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Mon Jan 5 17:24:51 2026 +0100

    tool/hid: don't generate top-level comment
    
    This patch omits comments preceeding the top-level node because
    such comments are (currently) not recognized by Genode's util/hid.h
    as valid HID data.
    
    Observed with repos/os/src/server/event_filter/en_us.chargen.
    
    Issue #5782

commit d5cc64b70ccf3341beccae071d6caae27a613eb2
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Tue Jan 6 13:59:36 2026 +0100

    tool/hid: attach line comments to preceeding node
    
    This patch adjusts the internal representation of HID data closer to the
    foreseen commenting style, which places the documentation of a node
    below the node declaration, giving the node declaration the purpose of
    a headline for the subsequent comments.
    
    Hence, comment lines are now attached to the preceding node, not the
    following node. The internal name of the field has been changed from
    'preface' to 'doc'.
    
    Issue #5782

commit 4691a06b17082ceb8e79efb5b6dc602c5ea1c32a
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Wed Jan 14 17:30:30 2026 +0100

    tool/hid: separate first non-name attr by |
    
    When a node has no name, put the first attribute on the next line to
    avoid the pipe character, which looks nicer when more attribute lines
    follow.
    
    When all attributes fit on the node line, join them via | separators.
    
    Issue #5788

commit 9bb8627628f79d7bafe2eff5aaa23bb1b2271bcc
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Thu Jan 15 14:43:15 2026 +0100

    tool/hid: distinguish doc from annotation for XML
    
    When importing XML, interpret comments appearing at the right side of a
    node as an annotion of the preceeding node, not as the documentation of
    the following node.
    
    This fixes the relation of trailing comments with tabular formatted
    nodes, as used in repos/os/src/server/event_filter/en_us.chargen.
    
    Issue #5782

commit 4400d9b8eb8ed136d2eab5540ca2770368845a10
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Thu Jan 15 17:51:59 2026 +0100

    tool/hid: errro when missing '|' before attribute
    
    Issue #5788

commit b07fb5fe983f280f48f6a43699e67baeb75dc394
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Fri Jan 16 14:56:32 2026 +0100

    tool/hid: add check command
    
    Fixes #5790

commit bbdbc9823b27ff62b193cb36b982cfe2e2b9e2c6
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Thu Jan 22 15:17:28 2026 +0100

    tool/hid: avoid superfluous newline before '-'

commit 6d9b97e72e68edf3f51e5aa149a894eede11017f
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Wed Jan 28 11:50:26 2026 +0100

    tool/hid: simplify parser
    
    The syntactic refinement of issue #5788 (always separating the first
    non-name attr by '|') lends itself to the simplification of the
    parser. This patch removes recursion by splitting each line into
    '|'-separated segments that are handled independently. This has
    the welcome side effect of making the parser more strict. I.e.,
    missing spaces around '|' delimiters are now reported as an error.
    
    Issue #5788

commit 48b65744b856b705fb0faf8bde61c23d79c0efb0
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Thu Dec 11 14:52:32 2025 +0100

    Vim syntax highlighting for HID and Genode log

commit 5e7c0b7c949f8143908fa1a384ac6eba1528f325
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Thu Aug 7 14:22:22 2025 +0200

    ldso: globally disable 'generate_xml' by default
    
    This patch changes the default mode of the 'Generator' to HID.
    For each individual component, the mode can still be switched back
    to XML by enabling the 'generate_xml' attribute at the top level
    of the component's config.
    
    Issue #5786

commit 9d7b895919693f91c3317f0d4417f246be4e613a
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Thu Jan 15 16:54:14 2026 +0100

    Hid_generator: separate first non-name attr by |
    
    Issue #5788

commit e0db8080ed94ece935ebb507d06d3956eee0172f
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Mon Jan 19 16:03:01 2026 +0100

    tool/depot: validate HID-based pkg/runtime files
    
    Issue #5790

commit f27ebe205df64007518d75541c0c6d35628b7f80
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Tue Jan 20 16:05:26 2026 +0100

    mk: pick up .hsd files as build artifacts
    
    Issue #5786

commit 49782e682f3f988057ac90365dd552b9f0ed801b
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Sun Jan 18 00:21:41 2026 +0100

    init: HID schema description
    
    Issue #5786

commit 1afa796cd03063ee5a4807b44a2e904732d3f935
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Tue Jan 20 17:04:20 2026 +0100

    tool/run: validate HID-formatted init configs
    
    Issue #5786

commit 9c9c322b675c68a4606dc4ecebf483dbb07d8b21
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Wed Jan 28 16:05:10 2026 +0100

    tool/run: validate inline-defined HID sub configs
    
    Issue #5790

commit ee480ccc34293a3c71831be0d62b199a3a67f746
Author: Norman Feske <norman.feske@genode-labs.com>
Date:   Wed Jan 28 14:34:11 2026 +0100

    nic_router: add nic_router.hsd

Migration from GitHub to Codeberg
#################################

As outlined in our [https://genode.org/about/road-map - road map],
we migrated all but the main (genode.git) repository from GitHub to Codeberg.
From now on, you will find all genode-* repositories as well as the Goa
repository at

  [https://codeberg.org/genodelabs]

Along with the git repositories, all issues, pull requests and comments have
been migrated as well. The repositories at GitHub have been made read-only and
therefore will not receive any updates. In the course of the migration, we have
also changed the name of the default branch from "master" to "main".

In order to adapt to these changes on your side, we advise you to switch to the
current master branch in your clone of https://github.com/genodelabs/genode.git
and follow the instructions that are printed by any build command. For Goa, you
can trigger the migration with the following commands:

! goa update-goa master
! goa versions

Note that the migration to Codeberg implies a few more changes for contributors:
Contributors must create an account at codeberg.org. It is possible to link
the new account to your GitHub account. This enables Codeberg to associate
your former comments (entered on GitHub) to your new account on Codeberg (i.e.
you may use different user names on both platforms). In case you forked any
repository on GitHub you should consider creating a new fork on Codeberg and
manually pushing your branches and tags to the new fork to restore the state of
the forked repository as it was on GitHub. The following instruction serve as
a guideline:

! git clone --bare https://github.com/<user>/<repo> bare_clone
! cd bare_clone
! git branch -m master main
! git remote set-url origin ssh://git@codeberg.com/<user>/<repo>
! git push --mirror

Codeberg uses Forgejo and has a very similar UI compared to GitHub. However,
there are a few changes when it comes to referencing issues and commits. On
Github, you are able to push a commit referencing an issue via '#<issue-number>'
to your fork and this commit will be shown in the corresponding issue of
the original repository from which you forked. On Codeberg, however, you
must explicitly mention the repository where the issue resides via, e.g.,
'genodelabs/goa#<issue-number>'. Similarly, if you want to reference a commit
from a fork in an issue comment, you must name the fork explicitly, e.g.
'jschlatow/goa@<commit-hash>'.


Base framework and OS-level infrastructure
##########################################

API refinements
===============

New 'Align' argument type
-------------------------

The 'Align' type defined at _util/misc_math.h_ is meant to be used as argument
for aligned allocations and replaces the former use of basic unsigned values.
Typical argument values AT_PAGE, AT_16_BYTES, AT_MWORD are now defined by the
public API, alleviating many former uses of 'get_page_size_log2()'.
In non-typical cases, the 'Align' name self-documents the purpose the argument
when passed around. When defining an alignment, the notion '{ .log2 = 10 }'
nicely highlights the meaning of 10 is an 2^n exponent.

Removal of memset
-----------------

Raw memory-clearing via 'memset' has arguably been a rarity throughout the
framework. As almost all of these calls used to specify 0 as fill character,
we have now replaced 'memset' by 'bzero'. The latter has a narrower interface
and no return value. Still, calls of 'bzero' rightfully remain a rarity.

Removed diag session argument
-----------------------------

Core's diag feature was a useful debugging aid during the introduction of
cap-quota accounting and trading around
[https://genode.org/documentation/release-notes/17.05#Assignment_and_trading_of_capability_quota version 17.05].
Given that the feature has remained unused ever since, we have now removed it.

Removed UART session interface
------------------------------

The UART session interface was introduced in
[https://genode.org/documentation/release-notes/12.11#New_UART_session_interface - version 12.11]
to extend the terminal session interface with the ability of setting the baud
rate. This was long before the configuration-based
[https://genode.org/documentation/genode-foundations/25.05/components/Component_configuration.html#Server-side_policy_selection - server-side policy selection]
became the predominant pattern for out-of-band concerns like this.
Since the split of session interfaces between terminal and UART is no longer
justified, the UART session got removed now.

VFS library
-----------

The VFS infrastructure at _include/vfs/_ has now become part of the 'Genode::'
namespace, which alleviates the need to prefix Genode types inside the 'Vfs'.

Each VFS file-system type now lives in a dedicated namespace prefixed
with 'Vfs_', which includes the 'Genode' and 'Genode::Vfs' namespaces.
This gives each plugin a natural place for custom types, flattening the
type hierarchy in cases where a compound file system is constructed.


Linux device-driver environment
===============================

This release features the first step in updating the used Linux version in
our device-driver environment to a recent 6.18 LTS release.

In particular, we started with updating all 'virt_linux'-based components.
This includes the TCP/IP stack, USB client device drivers like 'usb_hid',
'usb_net' and 'usb_serial', as well as the wireguard port.

Changes to the internal structuring within in Linux prompted us to adapt
our Linux emulation environment and guard those changes with kernel version
checks to keep them compatible with components that for moment still rely
on an older Linux release.

We also decided to drop support for ARMv6, which only concerns support for
the Raspberry Pi 1, as this platform has not seen much use in recent years
and the practical relevance is debatable.
On the other hand, the 'usb_serial' component is now also available for
ARMv8.

As of now, the update of 'virt_linux' to 6.18.5 is complete, while the
update of 'pc_linux' is at the starting block. We might consider bumping
the LTS version during this task to be closer to the current LTS release.


TCP/IP
======

; @ssumpf

commit 86071c26d43e4552311859542ebf353f0903d977
Author: Sebastian Sumpf <sebastian.sumpf@genode-labs.com>
Date:   Fri Nov 28 21:09:30 2025 +0100

    vfs/ip: add Error_file_system
    
    * Add and connect 'error' file for each socket
    
    * The 'error' file is a Single_file_system using Genode::Generator to
      create an 'error'-node with the 'name' set to the
      genode_c_api/socket.h minus GENODE_ and 'value' set the corresponding
      enum value
    
    issue #5768

commit ed1975a8fe44965f394a698e0aa92e56f9477050
Author: Sebastian Sumpf <sebastian.sumpf@genode-labs.com>
Date:   Tue Dec 2 13:37:08 2025 +0100

    libc/socket_fs: use error file
    
    * Retrieve Genode::Errno from 'error' file and translate it libc's errno
    
    * Adjust lwip to return the same results as lxip
    
    * Adjust tests to new errno's
    
    issue #5768

commit 87e512967809e8a6299c9386ae6619f22868f2dc
Author: Christian Prochaska <christian.prochaska@genode-labs.com>
Date:   Wed Oct 15 12:24:56 2025 +0200

    libc: test 'recv' with MSG_PEEK
    
    * add recv with MSG_PEEK flag set to libc_connect test
    
    Issue #5768

commit 62c4cfd1376be0231e6cbb496846cfe02ae569e2
Author: Sebastian Sumpf <sebastian.sumpf@genode-labs.com>
Date:   Wed Dec 3 20:26:49 2025 +0100

    depot_autopilot: add test-libc_udp_unreachable test
    
    Tests returned errno for unreachable network (Nic session is routed to
    the black_hole) when using UDP with lxip/lwip.
    
    issue #5768

commit 14241026ec1c4d33a9152d023acaafe7d51f13e3
Author: Sebastian Sumpf <sebastian.sumpf@genode-labs.com>
Date:   Wed Dec 3 21:36:21 2025 +0100

    lwip: Effective C++
    
    issue #5768


Event filter
============

The global-key Nitpicker feature facilitates the implementation of global
key combinations, for example, the window manager's KEY_SCREEN shortcuts. If
the global key is pressed all subsequent input events are passed to the
configured handler of the global key by Nitpicker until no key is pressed.
This effectively removes those input events from the event stream of the
current focused component. Thus, an outside observer can't detect key
combinations while leaving the event stream unmodified and usable by the
focused Nitpicker client. An example use case of such an observer is a
global screen lock that is activated if Win+L is pressed while the Win key
is also handled by clients (e.g., a virtual machine).

In this release, we enhanced the event-filter component to support reporting
of _shortcuts_ if configured key combinations are observed. The key events
are preserved for further processing (resp. not filtered out).

! report
! + shortcut screenshot
!   + key KEY_LEFTMETA
!   + key KEY_S
! + shortcut screenlock
!   + key KEY_LEFTMETA
!   + key KEY_L

The example configuration declares two shortcut reports named screenlock and
screenshot. Event filter creates two report sessions labeled by the shortcut
names and reports each occurrence of the combinations. The report provides
the shortcut 'name' and a unique 'serial' number as attributes like follows.

! shortcut screenshot | serial: 7

Report observers must record the last observed serial number for comparison
on later report updates. The first serial number and future changes of its
value shall be interpreted as shortcut events. Note, reports missing a valid
'serial' attribute must be ignored.


Software provided via the Genode-world repository
#################################################

; @alex-ab, @cproc

Housekeeping
------------

The runtime files of the Goa projects maintained within the
Genode-world repository were converted from XML to HID.

In addition, the currently used run-scripts for CI testing also got the
HID treatment and to make their usage more apparent were moved into the
Genode-ports repository. This legacy portion of Genode-world is
slated for removal in summer of 2026.

New port of Git
---------------

commit c7a5993e114680cd019dbf4e17a82e51c91c983c
Author: Christian Prochaska <christian.prochaska@genode-labs.com>
Date:   Mon Feb 16 11:45:43 2026 +0100

    add Git
    
    Fixes genodelabs/genode-world#425

Seoul VMM
---------

The Seoul VMM got converted to be usable with Goa. All Genode specific code
located in genode-world has been moved to the Seoul source repository and a
make-based build environment was crafted. The new
[https://codeberg.org/genodelabs/genode-world/src/branch/main/vmm/seoul - Goa project]
imports this modified Seoul repository and uses a 'Makefile' to build the VMM.

FIO benchmark
-------------

A port of the well-known _flexible I/O tester_ joins the Genode-world
repository. Among others, it is used to exercise the I/O aspects of
our POSIX runtime.


Platforms
#########

Execution on bare hardware (base-hw)
====================================

Genode's custom kernel now exports additional information when collecting
execution times of subjects in the system via the TRACE service. Beforehand
execution times of all threads got collected. Now when using hardware-assisted
virtualization, the cpu usage of virtual-cpus is tracked too. Moreover,
subject properties of the new scheduler introduced in
[https://genode.org/documentation/release-notes/25.08#Kernel_scheduling_for_fairness_and_low_latency
- release 25.08] are getting exposed additionally. Namely, the weights and warp
values of the individual threads and virtual-cpus.

The enduring effort to keep and enhance the quality of the bare hardware
runtime's code-base are going on. This time by reworking the kernel internal
interrupt representation, as well as the allocation of hardware-based address
space identifiers. A failure of the latter gets now reflected to upper layers of
the system.

Linux
=====

For test-driving interactive system scenarios on Linux, Genode employs the
[https://github.com/genodelabs/genode/tree/master/repos/os/src/driver/framebuffer/sdl - fb_sdl]
component as pseudo display and input driver. In
[https://genode.org/documentation/release-notes/23.11#Linux - version 23.11],
it received an update to [https://wiki.libsdl.org/SDL2 - SDL2]. The current
release [https://github.com/genodelabs/genode/pull/5784 - adds] principal
support for [https://wiki.libsdl.org/SDL3 - SDL3] in addition.
Whereas the time-tested version 2 is still used by default, version 3 can now
be selected by setting the environment variable GENODE_FB_SDL_VERSION to 3
at build time.


Build system and tooling
########################

Goa SDK
=======

Following the spirit of this release, we converted all examples provided
in the Goa repository from XML to HID, and made HID the new default.
Furthermore, we integrated schema validation of _runtime_ files, _index_ files,
runtime configs (against init.hsd) and component configs
(determined by depot-provided .hsd files).

With the move of genode-world towards Goa, we also reflected on the structure
of Goa's project directories. Since day one, Goa provided for multiple
pkg definitions in a single Goa project as subdirectories under _pkg/_,
which has rarely been used in practice. We therefore removed the substructure
of the _pkg/_ directory so that the _runtime_, _archives_ and _README_ files are
now supposed to be placed in the _pkg/_ directory directly.
In the meantime, Goa has been enabled to detect, build and export Goa projects
that are referenced as dependencies. Dependent runtimes can therefore be hosted
in arbitrary subdirectories instead. For instance, a _test/_ subdirectory
can be used for defining a Goa project as a test case for the parent project.
This test case can be run by executing 'goa run -C test' from the parent
directory. Note that the name and hierarchy of the subdirectory is arbitrary.
However, we actually decided to give any _test/_ directory a special meaning:
Whenever Goa is run recursively via 'goa -r', it ignores any project that
has a _/test/_ path element so that test cases can be easily ignored for
publishing. For testing, we added the '--only-tests' command-line switch that
inverts this logic.

With the scenario of nested Goa projects in mind, we also removed the
possibility to modify the project name and directory via _goarc_ files, which
did not make much sense anymore. If needed one may resort to symlinks instead.

Last but no least, we reflected on the way how Goa passes the required linker
flags and libraries to the 3rd-party build system. So far, we used environment
variables such as 'LDFLAGS' and 'LIBS' to achieve this. Yet, not all 3rd-party
projects, especially projects with custom _configure_ scripts, make use of
these. We eventually found
[https://gcc.gnu.org/onlinedocs/gcc/Spec-Files.html - GCC spec files]
to be a robust alternative to environment variables that reduces the need
for patching. We already applied the new approach to Goa's support for
autoconf, make, cmake, qmake and cargo.

Note that not all the aforementioned changes entered Goa's _25.10_ branch.
In order to maintain compatibility with Sculpt 25.10 and corresponding Goa
projects, we omitted the switch towards using HID by default and the
flattening of the _pkg/_ directory from the _25.10_ branch and only added it to
the _main_ branch. You may conveniently switch between both branches with
the following commands:

! goa update-goa main
! goa update-goa 25.10

Run-tool support for AMT TLS
============================

With newer versions of Intel's Active Management Technology (AMT)
using TLS is mandatory. So we extended our run-tool plugins that
deal with AMT to capture serial messages as well as to power-cycle
the machine in question.

The RUN_OPT flag '--wsman-tls' switches use of TLS on when set to
'yes', the default is 'no'.

The RUN_OPT flag '--amtterm-tls' switches use of TLS on when set to
'yes', the default is 'no'. As TLS SoL requires the host's certificate,
downloading and placing it into the build directory beforehand is vital.

The following command can be used to obtain the certificate:

!openssl s_client -connect <host>:16993 < /dev/null 2> /dev/null | openssl x509 > <host>.cert

It might also be necessary to active the serial redirection with. The
following command illustrates how to perform this operation using the 'wsman-cli'
program:

!wsman --https --endpoint https://admin:<password>@<host>:16993 \
!      --noverifypeer --noverifyhost \
!      put http://intel.com/wbem/wscim/1/amt-schema/1/AMT_RedirectionService \
!      -k ListenerEnabled=true
